# ai-zoll (CLI)

Node.js/TypeScript CLI: `init`, `analyze`, `generate`, `sync`, `login`. See
`docs/PRODUCT_SPEC.md` §22-23 and `docs/plan/03-roadmap.md` Phase 5.

**Status:** not yet implemented (placeholder package). Must remain independent of
`apps/web` (Rule 7) — authentication, repository analysis, local validation, blueprint
retrieval, and file generation only; heavy AI operations happen remotely via the API.
