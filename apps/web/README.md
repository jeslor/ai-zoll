# @ai-software-zoll/web

Next.js dashboard. See `docs/PRODUCT_SPEC.md` §6-11, 29-30 and
`docs/plan/03-roadmap.md` Phase 6.

**Status:** not yet implemented (placeholder package). Do not build this before
Phases 1-2 (Blueprint engine + deterministic generator) exist — the dashboard consumes
the same blueprint APIs as the CLI, it isn't the foundation.
