{
  "name": "@ai-zoll/agents",
  "version": "0.0.0",
  "private": true,
  "description": "AgentAdapter implementations: claude, cursor, codex, copilot, cline, zed.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-zoll/blueprint": "workspace:^",
    "@ai-zoll/generators": "workspace:^",
    "@ai-zoll/shared": "workspace:^"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
