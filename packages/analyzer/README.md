# @ai-zoll/analyzer

Deterministic repository analyzers (`PackageAnalyzer`, `FrameworkAnalyzer`,
`DatabaseAnalyzer`, `DirectoryAnalyzer`, `TestAnalyzer`, `GitAnalyzer`,
`ConfigurationAnalyzer`, `DependencyAnalyzer`, ...) for the existing-project workflow.
See `docs/PRODUCT_SPEC.md` §13-17 and `docs/plan/03-roadmap.md` Phase 7.

**Status:** not yet implemented (placeholder package). Also owns/enforces the
secret-exclusion rules from `docs/plan/05-security-and-privacy.md`. See
`.claude/skills/add-repo-analyzer/SKILL.md` before adding an analyzer.
