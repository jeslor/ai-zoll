{
  "name": "@ai-zoll/templates",
  "version": "0.0.0",
  "private": true,
  "description": "Raw templates consumed by packages/generators and packages/agents.",
  "main": "src/index.ts",
  "scripts": {
    "build": "echo \"[templates] deliberately empty — see README\"",
    "lint": "echo \"[templates] deliberately empty — see README\"",
    "test": "echo \"[templates] deliberately empty — see README\"",
    "typecheck": "echo \"[templates] deliberately empty — see README\""
  }
}
