{
  "name": "@ai-software-zoll/ai",
  "version": "0.0.0",
  "private": true,
  "description": "AIProvider abstraction: mock provider first, real provider later.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"[ai] lint tooling pending (Phase 0)\"",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-software-zoll/blueprint": "workspace:^",
    "@anthropic-ai/sdk": "^0.116.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
