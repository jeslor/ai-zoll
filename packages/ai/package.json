{
  "name": "@ai-zoll/ai",
  "version": "0.0.0",
  "private": true,
  "description": "AIProvider abstraction: MockAIProvider (default) and the real, --ai-gated ClaudeAIProvider.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "biome check src",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-zoll/analyzer": "workspace:^",
    "@ai-zoll/blueprint": "workspace:^",
    "@anthropic-ai/sdk": "^0.116.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
