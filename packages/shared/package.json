{
  "name": "@ai-software-zoll/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-cutting types and utilities with no framework dependency.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"[shared] lint tooling pending (Phase 0)\"",
    "test": "echo \"[shared] type-only package — no runtime tests yet\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^7.0.2"
  }
}
